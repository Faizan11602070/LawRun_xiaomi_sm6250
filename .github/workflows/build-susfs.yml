name: Build LawRun Kernel with SUSFS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout LawRun Kernel Source
        uses: actions/checkout@v4

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential curl flex git gnupg gperf imagemagick lib32z1-dev liblz4-tool libncurses-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev python3 python3-pip

      - name: Download and Extract Neutron Clang Toolchain
        run: |
          wget -q https://raw.githubusercontent.com/Neutron-Toolchains/antman/main/antman -O antman
          chmod +x antman
          ./antman -S
          echo "$GITHUB_WORKSPACE/neutron-clang/bin" >> $GITHUB_PATH

      - name: Clone KernelSU (non-GKI)
        run: |
          rm -rf KernelSU
          git clone https://github.com/tiann/KernelSU.git KernelSU
          cd KernelSU
          git checkout v0.9.5

      - name: Clone SUSFS Patches
        run: |
          rm -rf susfs4ksu
          git config --global http.postBuffer 524288000
          git config --global http.lowSpeedLimit 0
          git config --global http.lowSpeedTime 999999
          for i in 1 2 3 4 5; do
            git clone --depth=1 --branch kernel-4.14 https://gitlab.com/simonpunk/susfs4ksu.git susfs4ksu && break
            echo "Attempt $i failed, retrying..."
            rm -rf susfs4ksu
            sleep 5
          done
          test -d susfs4ksu || { echo "All clone attempts failed"; exit 1; }
          cd susfs4ksu
          git log --oneline -1

      - name: Apply KernelSU Patches
        run: |
          cd $GITHUB_WORKSPACE
          bash KernelSU/tools/init_kernelsu.sh $(pwd) || echo "KernelSU init script failed. Manual integration required."

      - name: Apply SUSFS Patches
        run: |
          cd $GITHUB_WORKSPACE
          cp susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch KernelSU/
          cp susfs4ksu/kernel_patches/50_add_susfs_in_kernel-4.14.patch ./
          cp -r susfs4ksu/kernel_patches/fs/* fs/
          cp -r susfs4ksu/kernel_patches/include/linux/* include/linux/
          cd KernelSU
          patch -p1 < 10_enable_susfs_for_ksu.patch || echo "KERNELSU SUSFS PATCH FAILED"
          cd ..
          patch -p1 < 50_add_susfs_in_kernel-4.14.patch || echo "KERNEL SUSFS PATCH FAILED"

      - name: Prepare Defconfig (Remove Duplicates, Append Correctly)
        run: |
          DEFCONFIG=arch/arm64/configs/vendor/atoll-perf_defconfig
          # Remove any pre-existing entries to avoid duplicate/conflicting values
          sed -i '/CONFIG_LITTLE_CPU_MASK/d' $DEFCONFIG
          sed -i '/CONFIG_BIG_CPU_MASK/d' $DEFCONFIG
          sed -i '/CONFIG_KSU/d' $DEFCONFIG
          # Append with quotes for string-type symbols (LITTLE/BIG_CPU_MASK)
          echo 'CONFIG_KSU=y' >> $DEFCONFIG
          echo 'CONFIG_KSU_SUSFS=y' >> $DEFCONFIG
          echo 'CONFIG_LITTLE_CPU_MASK="0xF"' >> $DEFCONFIG
          echo 'CONFIG_BIG_CPU_MASK="0xF0"' >> $DEFCONFIG

      - name: Configure Kernel (Fully Non-Interactive)
        run: |
          export ARCH=arm64
          export SUBARCH=arm64
          # Step 1: Generate the .config from the defconfig
          make O=out ARCH=arm64 vendor/atoll-perf_defconfig
          # Step 2: Force-set the string-type symbols via scripts/config as a safety net
          ./scripts/config --file out/.config --set-str LITTLE_CPU_MASK 0xF
          ./scripts/config --file out/.config --set-str BIG_CPU_MASK 0xF0
          ./scripts/config --file out/.config --enable KSU
          ./scripts/config --file out/.config --enable KSU_SUSFS
          # Step 3: Finalize without prompting, feeding /dev/null to any remaining prompts
          make O=out ARCH=arm64 olddefconfig </dev/null
          # Step 4: Verify values are set
          grep -E "LITTLE_CPU_MASK|BIG_CPU_MASK|CONFIG_KSU" out/.config || true

      - name: Build Kernel
        run: |
          export ARCH=arm64
          export SUBARCH=arm64
          export KBUILD_HOSTLDFLAGS="-fuse-ld=lld"
          make -j$(nproc --all) O=out ARCH=arm64 LLVM=1 KBUILD_HOSTLDFLAGS="-fuse-ld=lld"

      - name: Package with AnyKernel3
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git -b master AnyKernel3
          cp out/arch/arm64/boot/Image.gz-dtb AnyKernel3/
          cd AnyKernel3
          zip -r9 ../LawRun-SUSFS-miatoll.zip *

      - name: Upload Kernel Zip
        uses: actions/upload-artifact@v4
        with:
          name: LawRun-SUSFS-miatoll
          path: LawRun-SUSFS-miatoll.zip
